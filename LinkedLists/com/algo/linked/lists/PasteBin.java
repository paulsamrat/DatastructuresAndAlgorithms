package com.algo.linked.lists;

public class PasteBin {
	/**
	 * 
	 * Pastebin is a service that lets users post various types of content, such
	 * as text and images, on the internet and share them with others using a
	 * unique URL. The creator of the content can also update it if they are logged in.
	 * 
	 * Functional Requirements:
	 * 1. Users should be able to generate a unique URL by pasting content into the system.
	 * 2. The content and URLs should expire after a specified amount of time

	 *  NFR:  Reliable (service will operate under various real-world circumstances.), 
	 *  Available, URL should not be guessable.
	 *  
	 * 
	 */
}
